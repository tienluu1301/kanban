import { _id, _fullNames } from './assets';

// ----------------------------------------------------------------------

export const _mock = {
  id: (index: number) => _id[index],

  fullName: (index: number) => _fullNames[index],
};
